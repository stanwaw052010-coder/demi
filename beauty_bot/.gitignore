# Секреты
.env
# ...но образец без секретов нужен в репозитории
# (корневой .gitignore исключает .env*, поэтому возвращаем явно)
!.env.example

# База данных
bot.db
bot.db-shm
bot.db-wal

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
.mypy_cache/
.ruff_cache/
