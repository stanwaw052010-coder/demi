/**
 * There is no tea house yet.
 *
 * Well's of Yunnan sells online only until the room opens, so everything on
 * this site that invites someone to turn up somewhere — collecting an order at
 * a counter, booking a seat at a tasting, opening hours, a cabinet you can come
 * and look at — is switched off from this one constant rather than deleted.
 * The pages stay, the promises do not.
 *
 * On the day the door opens: set this to true, put the real address back in
 * COMPANY.registered (or add a separate `shop`), and fill content/tastings.ts
 * with real sessions. Everything below follows.
 */
export const VENUE_OPEN = false;
