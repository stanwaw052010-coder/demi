/**
 * Photos that actually exist under /public today. `Photo` renders the real
 * file for any path listed here and a branded placeholder for everything
 * else — add a path here the moment you drop the matching file in (see
 * PHOTOS.md for the expected filenames), no other code changes needed.
 */
export const AVAILABLE_IMAGES = new Set<string>([]);
